#!/bin/sh

# Creating/enabling varlog service
ln -s /home/pi/emonpi/logtools/oem-varlog.service /etc/systemd/system

# Planned job to backup logfile every hours
if [ ! -f /etc/cron.hourly/oem-varlog ]; then
    sudo ln -s /home/pi/emonpi/logtools/oem-varlog.cron /etc/cron.hourly/oem-varlog
fi
